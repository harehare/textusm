version: "2"
cloud:
  project: "textusm"
sql:
  - schema: "db/migrations"
    queries: "db/query.sql"
    engine: "postgresql"
    gen:
      go:
        package: "db"
        out: "internal/db"
    rules:
      - sqlc/db-prepare
